#!/usr/bin/env python
from xmlrpclib import ServerProxy
from netways_ingraph import utils

config = utils.load_config('grapher-xmlrpc.conf')
url = utils.get_xmlrpc_url(config)
api = ServerProxy(url)

print("Sending shutdown() call to the Grapher daemon...")
api.shutdown()
print("Done.")