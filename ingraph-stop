#!/usr/bin/env python
from xmlrpclib import ServerProxy
from ingrah import utils

config = utils.load_config('ingraph-xmlrpc.conf')
url = utils.get_xmlrpc_url(config)
api = ServerProxy(url)

print("Sending shutdown() call to the inGraph daemon...")
api.shutdown()
print("Done.")